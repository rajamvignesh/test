name: Run Python Tests
on:
  push:
    branches:
      - test_branch

# env:
#   deploy_status_value: "none"

jobs:
  build_and_test:
    name: Build and test
    runs-on: ubuntu-latest
    outputs: 
      deploty_status_val : ${{steps.Deploy_status.outputs.deploy_status_value}}
    steps:
      - uses: actions/checkout@v2
      - name: Install Python 3
        uses: actions/setup-python@v1
        with:
          python-version: 3.6
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-html

      - name: Get Env Variables
        run: |
          echo "LM_URL=${{ secrets.LM_URL }}" >> $GITHUB_ENV
          echo "DASHBOARD_NAME=${{ secrets.DASHBOARD_NAME }}" >> $GITHUB_ENV

      - name: Build coverage file
        run: |
          pytest 
          pytest -v --junitxml=./result.xml
          pytest --html=./report.html
          pytest -v --junitxml=C:\personal\test\result.xml

      - name: Publish Test Results
        uses: EnricoMi/publish-unit-test-result-action@v1
        if: always()
        with:
          files: "/home/runner/work/test/test/*.xml"

      - name: Comment coverage
        uses: coroo/pytest-coverage-commentator@v1.0.2

        # - name: Build coverage file
        #   run: |
        #     pytest --junitxml=pytest.xml --cov-report=term-missing:skip-covered --cov=app tests/ | tee pytest-coverage.txt

      # - name: Generate Report
      #   run: |
      #     pip install coverage
      #     coverage run -m unittest
      # - name: Upload Coverage to Codecov
      #   uses: codecov/codecov-action@v2

      # - name: Get Deploy status
      #   if: always()
      #   id: Deploy_status
      #   run: |
      #     echo "::set-output name=deploy_status_value::${{ job.status }}"  

      # - name: Dump steps context
      #   env:
      #     STEPS_CONTEXT: ${{ toJson(steps) }}
      #   run: echo---------------- "$STEPS_CONTEXT" 

      # - name: Check the git build status
      #   if: job.status == 'success'
      #   run: |
      #     echo "test case passed"    

  # email_notification:
  #   name: EMAIL TESTING
  #   if: always()
  #   needs: build_and_test
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Send mail
  #       uses: dawidd6/action-send-mail@v3
  #       with:
  #         server_address: eu-smtp-outbound-1.mimecast.com
  #         server_port: 25
  #         username: airflow.awx@global.ntt
  #         password: ${{ secrets.SMPT_PASSWORD }}
  #         subject: Deployment of ${{ github.repository }} has ${{ needs.build_and_test.outputs.deploty_status_val }}
  #         body: Deployment job in of ${{ github.repository }} has ${{ needs.build_and_test.outputs.deploty_status_val }}
  #         html_body: |
  #             <!DOCTYPE html>
  #             <html>
  #             <body>
  #             <h2>Message from Github Action</h2>
  #             <p>Deployment Status : ${{ needs.build_and_test.outputs.deploty_status_val }} </p>
  #             </body>
  #             </html>
  #         to: vigneshraja.muruganantham@global.ntt
  #         from: github-actions
  #         ignore_cert: true
  #         convert_markdown: true
  #         priority: low   

  # output:
  #   runs-on: ubuntu-latest
  #   needs: build_and_test
  #   steps:
  #     - name: Try to get deploy status 
  #       run: |
  #         echo values --------- "${{ needs.build_and_test.outputs.deploty_status_val }}"

# jobs:
#   job1:
#     runs-on: ubuntu-latest
#     # Map a step output to a job 
#     outputs: 
#       output1: ${{steps.step1.outputs.test}}
#       output2: ${{steps.step2.outputs.test}}
#     steps:
#       -id: step1
#        run: echo"::set-output name=test::hello"
#       -id: step2
#        run: echo"::set-output name=test::world"
#   job2:
#     runs-on: ubuntu-latest
#     needs: job1
#     steps: 
#       -run: echo${{needs.job1.outputs.output1}}$ {{needs.job1.outputs.output2}}

  #     - name: Dump steps context
  #       env:
  #         STEPS_CONTEXT: ${{ toJson(steps) }}
  #       run: echo "$STEPS_CONTEXT"


          # echo "::set-output name=env_url::$ENV_URL"
          # echo ::set-output name=deploy_status_value::$deploy_status_value
          # echo "$deploy_status_value"

# echo "${{ toJson(github) }}"

# jobs:
#   version_job:
#     name: Create version number
#     runs-on: ubuntu-latest
#     steps:
#     - id: version_step
#       run: echo "::set-output name=version_number::1.2.3"

#   output:
#     name: Output
#     runs-on: ubuntu-latest
#     needs: version_job
#     steps:
#     - run: echo "${{ jobs.version_job.steps.version_step.outputs.version_number }}"

